---
# Install with: ansible-galaxy collection install -r requirements.yml
# (The full `ansible` package already bundles ansible.posix; this is for hosts
# that installed ansible-core only.)
collections:
  - name: ansible.posix  # synchronize (rsync) module used by deploy_services_ramus_mcp.yml
