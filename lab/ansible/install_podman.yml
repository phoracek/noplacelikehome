---
- name: Install Podman and prepare host for Quadlet-managed containers
  hosts: server
  become: yes
  tasks:
    - name: Enable EPEL (nss-mdns lives there on AlmaLinux 9)
      ansible.builtin.dnf:
        name: epel-release
        state: present

    - name: Install podman, avahi, and nss-mdns
      # nss-mdns is what lets the host's own libc resolve home.local
      # back to itself. avahi alone only *advertises* the name to the
      # LAN — without nss-mdns the host can't resolve its own .local
      # name, so anything on the host that dials a service by that name
      # fails with "no such host".
      ansible.builtin.dnf:
        name:
          - podman
          - firewalld
          - avahi
          - nss-mdns
          - chrony
        state: present

    - name: Enable chronyd (keeps the host clock in sync)
      ansible.builtin.systemd_service:
        name: chronyd.service
        state: started
        enabled: true

    - name: Enable avahi-daemon (advertises <hostname>.local via mDNS)
      ansible.builtin.systemd_service:
        name: avahi-daemon.service
        state: started
        enabled: true

    - name: Wire mdns4_minimal into /etc/nsswitch.conf hosts line
      # The nss-mdns RPM doesn't touch nsswitch on its own; without
      # this line the .so is installed but never consulted. The
      # negative lookahead keeps it idempotent.
      ansible.builtin.replace:
        path: /etc/nsswitch.conf
        regexp: '^(hosts:\s+files)(?!.*mdns4_minimal)(.*)$'
        replace: '\1 mdns4_minimal [NOTFOUND=return]\2'

    - name: Enable podman auto-update timer
      ansible.builtin.systemd_service:
        name: podman-auto-update.timer
        state: started
        enabled: true

    - name: Create homelab data directories
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        owner: root
        group: root
        mode: "0755"
      loop:
        - /var/lib/homelab
        - /var/lib/homelab/homeassistant
        - /var/lib/homelab/zigbee2mqtt
        - /var/lib/homelab/clouds-over-czechoslovakia
        - /var/lib/homelab/dashy
        - /var/lib/homelab/mosquitto
        - /var/lib/homelab/mosquitto/config
        - /var/lib/homelab/mosquitto/data
        - /var/lib/homelab/mosquitto/log

    - name: Install Dashy configuration
      ansible.builtin.copy:
        src: files/dashy/conf.yml
        dest: /var/lib/homelab/dashy/conf.yml
        owner: root
        group: root
        mode: "0644"
      notify: Restart dashy

    - name: Install mosquitto configuration
      ansible.builtin.copy:
        src: files/mosquitto/mosquitto.conf
        dest: /var/lib/homelab/mosquitto/config/mosquitto.conf
        owner: root
        group: root
        mode: "0644"
      notify: Restart mosquitto

    - name: Own mosquitto data/log to the in-container mosquitto user (UID 1883)
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        owner: "1883"
        group: "1883"
        mode: "0755"
        recurse: true
      loop:
        - /var/lib/homelab/mosquitto/data
        - /var/lib/homelab/mosquitto/log
      notify: Restart mosquitto

    - name: Seed Zigbee2MQTT configuration (skips the v2 onboarding wizard)
      ansible.builtin.copy:
        src: files/zigbee2mqtt/configuration.yaml
        dest: /var/lib/homelab/zigbee2mqtt/configuration.yaml
        owner: root
        group: root
        mode: "0644"
        force: false
      notify: Restart zigbee2mqtt

    - name: Install udev rule for Zigbee dongle
      ansible.builtin.copy:
        src: files/udev/99-zigbee.rules
        dest: /etc/udev/rules.d/99-zigbee.rules
        owner: root
        group: root
        mode: "0644"
      register: zigbee_udev

    - name: Reload udev rules
      ansible.builtin.command: udevadm control --reload-rules
      when: zigbee_udev.changed

    - name: Trigger udev for tty subsystem
      ansible.builtin.command: udevadm trigger --subsystem-match=tty
      when: zigbee_udev.changed

    - name: Ensure firewalld is running
      ansible.builtin.systemd_service:
        name: firewalld
        state: started
        enabled: true

    - name: Open firewall ports for the homelab services
      ansible.posix.firewalld:
        port: "{{ item }}"
        permanent: true
        immediate: true
        state: enabled
      loop:
        - 80/tcp
        - 8123/tcp
        - 8124/tcp
        - 8125/tcp
        - 61208/tcp

    - name: Allow mDNS through firewalld so home.local resolves on the LAN
      ansible.posix.firewalld:
        service: mdns
        permanent: true
        immediate: true
        state: enabled

  handlers:
    - name: Restart dashy
      ansible.builtin.systemd_service:
        name: dashy.service
        state: restarted
      failed_when: false

    - name: Restart mosquitto
      ansible.builtin.systemd_service:
        name: mosquitto.service
        state: restarted
      failed_when: false

    - name: Restart zigbee2mqtt
      ansible.builtin.systemd_service:
        name: zigbee2mqtt.service
        state: restarted
      failed_when: false
